# Scripts

This directory is reserved for small operational scripts that wrap package functionality.

Do not implement full benchmark experiments here; scripts should remain thin entry points over importable code in `src/hib/`.
